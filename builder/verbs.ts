export function _fetch<T>(basePath: string): Promise<T> {
  return <Promise<T>>Promise.resolve(true)
}

export function _remove<T>(basePath: string): Promise<T> {
  return <Promise<T>>Promise.resolve(true)
}

export function _create<T>(basePath: string): Promise<T> {
  return <Promise<T>>Promise.resolve(true)
}

export function _update<T>(basePath: string): Promise<T> {
  return <Promise<T>>Promise.resolve(true)
}
